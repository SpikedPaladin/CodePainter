install_data(
  app_id + '.svg',
  install_dir: get_option('prefix') / datadir / 'icons' / 'hicolor' / 'scalable' / 'apps',
)

install_data(
  app_id + '-symbolic.svg',
  install_dir: get_option('prefix') / datadir / 'icons' / 'hicolor' / 'scalable' / 'apps',
)

